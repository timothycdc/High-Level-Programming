// Why are these examples invalid?

// 1. addOne 12.0 => 13
// ans: returns int, so must accept int, but float given instead

// 2. "12" stringToInt => 12
// Can't apply string, it is not a function, need to feed it behind